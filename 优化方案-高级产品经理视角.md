# product-review-expert 优化方案（高级产品经理视角）

> 目的：在动手改文件前，先对齐诊断结论与改造方案。范围覆盖 3 个 skill（product-audit / logic-defect-review / interaction-review）+ senior-product-reviewer agent。

---

## 一、现状诊断：当前 skill 是「检查清单」，不是「审查专家」

读完三个 skill 后，核心判断一句话：**它们告诉模型"看哪些点"，但没告诉模型"怎么思考、怎么定级、怎么给出能落地的结论"**。这是新手 reviewer 和资深 reviewer 的本质差别——新手对着 checklist 逐条打勾，资深 reviewer 先理解业务意图，再用框架推演哪里会断。

具体六个问题：

| # | 问题 | 现状 | 后果 |
|---|------|------|------|
| 1 | **三者定位重叠** | product-audit 的维度 = logic + interaction 的并集，完全是超集 | 用户不知道何时用哪个；维护时改一处要同步三处 |
| 2 | **清单 ≠ 方法论** | 全是"是否完整/是否清晰"的罗列，没有思考框架 | 结果靠模型自由发挥，同一份 PRD 两次审查结论会漂移，不稳定 |
| 3 | **没有"先问后审"** | 拿到输入直接开喷 | 不知道业务目标和北极星指标，容易批错重点（把 P2 当 P0） |
| 4 | **输出不可落地** | 问题不锚定原文、不量化影响、建议停留在"建议优化体验" | 产品同学拿到报告仍不知道改哪行、改了能带来什么 |
| 5 | **定级无统一标准** | P0/P1/P2 只在 description 里给了文字定义 | 凭感觉定级，不同输入之间不可比 |
| 6 | **维度有缺口** | 只覆盖逻辑 + 交互 | 缺：数据埋点/可观测性、增长指标影响、合规与资损、可访问性、跨团队依赖、上线就绪度（灰度/回滚） |

---

## 二、改造方案

### 1. 三个 skill 重新差异化定位（解决问题 1）

| Skill | 新定位 | 何时用 |
|-------|--------|--------|
| `product-audit` | **全面体检 + 编排入口**：先做 intake，再按需调起逻辑/交互专项，输出综合结论 | 默认入口、上线前整体评审 |
| `logic-defect-review` | **逻辑/流程深挖专项**：状态机、闭环、并发、权限、规则冲突 | 只关心业务逻辑正确性时 |
| `interaction-review` | **体验/交互深挖专项**：信息架构、路径、反馈、表单、误操作 | 只关心 UX 时 |

做法：把共享的"方法论 + 定级标准 + 输出格式"抽成一段公共规范，三个 skill 各自只保留**专属深挖维度**，product-audit 引用而非复制另外两者。消除冗余。

### 2. 注入审查方法论（解决问题 2）

把"逐条检查"升级为"用框架推演"，在 skill 里显式写入这套思考链路：

1. **用户旅程拆解**：先还原"用户/角色想完成什么任务（JTBD）"，沿主路径走一遍，再走异常路径——异常路径优先级高于正常路径。
2. **状态机映射**：列出对象的所有状态与流转事件，检查是否有**未定义状态**、**无法到达状态**、**无法退出状态**。
3. **角色 × 权限矩阵**：把角色和关键操作摆成矩阵，逐格问"该角色在该状态下能否做这个操作"。
4. **断层检查**：前台允许但后台不支持 / 后台支持但前台无入口。
5. **统一定级公式**：

   > **严重度 = 影响面 × 发生概率 × 可逆性（上线后能否补救）**
   > - P0：核心链路断裂 / 资损 / 不可逆 / 高概率触发
   > - P1：影响核心体验或转化，但有 workaround
   > - P2：效率与体验优化，不影响主流程

### 3. 先问后审：加入 intake 门槛（解决问题 3）

审查正文前，先确认（信息已给则跳过，未给则简短追问 1 次或显式假设）：

- 业务目标 / 北极星指标是什么？
- 目标用户与核心场景？
- 本次上线范围（是否 MVP、哪些是后续迭代）？
- 已知约束（技术、合规、时间、历史包袱）？
- 这次最想让我重点审什么？

**关键原则**：信息不足时不沉默也不瞎猜——列出关键假设、标注"基于假设 X"，并把**缺失的关键定义本身当作一条风险项**列出来（资深 reviewer 的标志动作）。

### 4. 输出更可落地（解决问题 4 + 5）

每个问题条目从 6 字段升级为带证据和验证的结构：

| 字段 | 旧 | 新 |
|------|----|----|
| 证据锚点 | 无 | **引用原文片段**，或明确写"文档未定义 X" |
| 影响 | "影响体验" | **量化**：影响哪个指标、约多少用户/订单、最坏情况 |
| 修改建议 | "建议优化" | **具体方案**：改成什么样、给 1–2 个可选方案 |
| 验证方式 | 无 | 怎么确认改好了 / 能否 A/B / 看哪个指标 |
| 定级 | P0/P1/P2 | P 级 + **一句定级理由**（套用公式） |

### 5. 补充缺失维度（解决问题 6）

在 product-audit 里增设一组"资深维度"复查（按场景裁剪，不是每条都强答）：

- **数据与可观测性**：关键动作是否埋点？核心漏斗能否监控？异常是否告警？
- **增长与业务指标**：方案对转化 / 留存 / GMV 的预期影响方向？
- **合规与资损**：隐私、权限审计、可逆性、是否有资损敞口？
- **可访问性 / 国际化**：多语言、时区、无障碍。
- **跨团队 / 系统依赖**：上下游接口、时序依赖、谁的活儿没排上。
- **上线就绪度**：灰度、回滚、降级、容量预案。

---

## 三、建议的新输出结构（product-audit）

```
0. 审查范围与假设（intake 结果 / 我做了哪些假设）
1. 一句话结论 + Go / No-Go
2. 关键问题清单（P0 → P2）
   每条：标题 | 证据锚点 | 影响（量化）| 修改建议 | 验证方式 | 定级+理由
3. 高风险流失 / 资损点（单独拎出最致命的几个）
4. 缺失信息与待确认项
5. 结构性优化建议（成体系，非零散）
6. 上线就绪度判断：建议立即修改 / 优化后推进 / 可进入下一阶段
```

---

## 四、一个问题条目「改造前 vs 改造后」示例

**改造前（当前 skill 会产出的风格）：**

> 退款流程缺少异常处理，建议补充异常场景的提示，优化用户体验。

**改造后：**

> **[P0] 退款发起后第三方支付回调失败时，订单状态无定义**
> - **证据**：PRD 第 3.2 节只定义了"退款成功/退款中"两个状态，未定义回调超时或失败的状态。
> - **影响**：用户钱已扣、退款卡在"退款中"无法收敛，预计影响支付链路约 0.5%–2% 的退款单，直接触发客诉与资损争议。
> - **修改建议**：新增"退款异常"状态 + 定时对账补偿任务；前台展示"处理中，预计 X 小时"并提供客服入口。
> - **验证方式**：模拟回调超时/失败用例，确认状态可收敛；上线后监控"退款中超过 N 小时"的单量。
> - **定级理由**：影响面广 × 高概率（第三方回调本就不稳定）× 涉及资金不可逆 → P0。

---

## 五、落地清单（你确认后我执行）

1. 抽出公共「方法论 + 定级 + 输出格式」规范，三个 skill 引用，消除重叠。
2. 三个 skill 各补差异化深挖维度 + intake 门槛 + 可落地输出字段。
3. product-audit 增补 5 类资深维度复查。
4. 同步更新 agent（senior-product-reviewer）的编排逻辑与 README 的能力描述。
5. 不动 hook / 隐私 / 许可证等与本次无关的文件；版本号建议升到 1.1.0 并补 CHANGELOG。

> 是否需要保留中英双语？当前 skill 正文是纯中文、description 中英混排。建议正文保持中文、description 维持中英以利触发——如需全双语请告知。
